# TemPad

Reconstructs work history (hour by hour) from git, Monday.com and Claude Code session transcripts into SQLite, then generates timesheet reports.

## Layout

- `packages/core` — TypeScript library + `tempad` bin: schema, collectors, reports. Bun runtime, `bun:sqlite`.
- `apps/` — runnable programs. A Go TUI (charmbracelet) is planned.
- `libs/` — private bundle-time packages. Never published.
- `docs/specs/` — design specs. Read the relevant spec before changing a subsystem.

Runtime data never lives in the repo. `TEMPAD_HOME` (default `~/.tempad`) holds `.env`, `tempad.toml`, `tempad.db`, `repos/`, `reports/`.

## Commands

```sh
bun install
bun run lint          # biome check
bun run fmt           # biome + dprint write
bun run typecheck
bun test
moon run :test        # same through moon
```

## Conventions

- Package manager: bun. Never npm.
- Biome for JS/TS/JSON. dprint for Markdown/TOML/YAML. Go uses gofmt.
- Full words in identifiers (`repository`, not `repo`, in code; `repos/` as a directory name is fine).
- Environment variables that are required have no defaults; missing ones throw at startup.
- Tests in `bun test`, colocated as `*.test.ts`. Fixtures under `packages/core/test/fixtures`.
- Commits: `type(scope): summary`.

## CLI

- `tempad sync [monday|github|claude] [--full]` — `--full` clears that source's `sync_state` row before syncing, so the collector ignores the last-sync cursor and rescans everything back to `SINCE` (or, for Claude, re-reads every session file regardless of mtime). Use it to backfill columns added by a migration (e.g. `title_source`) on rows a normal incremental sync wouldn't touch.
- `tempad report <daily|project|hourly|weekly> --from <date> --to <date> [--org X] [--project Y] [--out path] [--as-of <iso>] [--party <slug>] [--client <slug>]`
- Intent layer: `tempad hero init`, `tempad party add|leave|list`, `tempad client add`, `tempad saga add|reword|replace|end|edit|list`, `tempad quest add|reword|replace|end|edit|confirm|merge|pause|resume|done|abandon|branch|return|list|declare`, `tempad stint list`, `tempad trace list`, `tempad answer`, `tempad rebuild [--until <iso>]`. See "Intent layer" below.
- w5 hook: `tempad w5 enqueue --session <id> [--forced]`, `tempad w5 context --session <id>`, `tempad w5 run [--detached]`, `tempad w5 hook install|uninstall [--scope user|project]`, `tempad w5 dedupe [--dry-run]`, `tempad quiet <2h|30m>`, `tempad review`. See "w5 hook" below.

## Intent layer

Hero, parties, clients, sagas, quests, stints, traces and questions are event-sourced: `packages/core/src/intent/`.

- **Events are append-only.** One `events` table (`packages/core/src/db/migrations/0003_events.sql`) is the source of truth; SQLite triggers reject `UPDATE`/`DELETE` on it. A wrong fact is corrected by a later event, never by editing history.
- **Projections are rebuildable.** Current-state tables (`heroes`, `parties`, `memberships`, `clients`, `sagas`, `quests`, `stints`, `traces`, `trace_links`, `questions`) are plain SQLite tables derived from events by pure reducers in `src/intent/projections/*.ts`. `tempad rebuild [--until <iso>]` truncates and replays them; this is always safe to run. Rebuilding `--until <date>` leaves the live projections showing state as of that date — the command prints `projections now reflect state as of <date>; run tempad rebuild to restore` as a reminder to re-run `tempad rebuild` with no `--until` afterward.
- **Edit intent rule.** The CLI refuses a bare edit (`saga edit` / `quest edit`) on a saga or quest that has attachments (a quest on a saga, a stint on a quest) — the caller must instead use `reword` (same id, new revision) or `replace` (new id, old one ended with `reason: "replaced"`), both of which are allowed even with attachments since they express an explicit wording- or meaning-change intent. The refusal message names the real subcommands, e.g. `saga <id> has attachments; use tempad saga reword <id> "<title>" or tempad saga replace <id> "<title>" --reason ...`. See `src/intent/edit-intent.ts`.
- **Time travel.** `stateAsOf(database, until)` (`src/intent/time-travel.ts`) rebuilds projections into a fresh in-memory database from events up to a date; `saga list --as-of <iso>` and `quest list --as-of <iso>` use it to answer "what were my sagas in August".

## w5 hook

Claude Code hooks (`Stop`, `PreCompact`, `SessionEnd`, `UserPromptSubmit`) enqueue classification jobs without ever blocking a session: `packages/core/hooks/w5-stop.sh` and `w5-prompt.sh` shell out to `tempad w5 enqueue`/`tempad w5 context` and always exit 0, logging failures to `TEMPAD_HOME/logs/w5.log` instead of surfacing them. `tempad w5 hook install|uninstall` (`src/w5/hooks.ts`) merges these into `~/.claude/settings.json` (or `.claude/settings.json` with `--scope project`) alongside any other hooks, marking its own entries so uninstall only removes those.

A detached `tempad w5 run` drains queued jobs: for each it re-syncs the one session file, builds a window of messages since the last run (`src/w5/window.ts`), classifies it, and applies the result as intent events (`src/w5/apply.ts`) — reusing or opening stints, proposing unconfirmed quests, branching on a detected switch. `src/w5/questions.ts` runs the hand-back rules: a question watches for `watchTurns` turns before it's asked, `why` questions on a stint that already has a quest expire straight to `tempad review` instead of asking, and an ask budget plus `tempad quiet <2h|30m>` keep it from asking too often.

**Classifier backend.** `[w5].backend` in `tempad.toml` is `"claude-cli"` (default) or `"api"`. `"claude-cli"` (`src/w5/classifier-cli.ts`, `ClaudeCliClassifier`) shells out to the local `claude -p` CLI in print mode with `--safe-mode --no-session-persistence --output-format json --tools ""`, prompt on stdin, cwd forced to `TEMPAD_HOME` (never the repo — no project `CLAUDE.md` or hooks leak in), so classification rides the operator's Claude subscription and never needs `ANTHROPIC_API_KEY`. `[w5].claude_command` overrides the binary name/path (default `"claude"`). `[w5].timeout_seconds` (default `180`) bounds a single classify request for both backends — the CLI backend kills the child process and the API backend aborts the fetch after this many seconds; `tempad w5 backfill` retries a window once on any classify failure (on top of the classifier's own JSON retry) before counting it failed. `"api"` (`src/w5/classifier.ts`, `AnthropicClassifier`) keeps the original behavior: POSTs to the Anthropic Messages API and requires `ANTHROPIC_API_KEY`. Both backends share the JSON-extraction/validate/retry-once loop in `src/w5/classifier-shared.ts`. `ANTHROPIC_API_KEY` is only ever required when the backend is `"api"`; `loadConfig` never requires it, so hooks run fine without it (they just log and no-op) and the default `claude-cli` backend never touches it.

**Backfill order.** Sessions are read ordered by `started_at` ascending, and every (session, chunk) pair is collected into one list sorted by chunk start, then classified in that order -- windows run chronologically **across** sessions, not one session to its end before the next begins. Two sessions running the same afternoon interleave in real time, and processing them session-by-session showed a window work that, from its own point in time, had not happened yet. Each session's per-chunk `sinceTs` chain still holds: a chunk's overlap tail comes from its own session's previous chunk, never from whichever window happened to run just before it.

**Backfill resilience.** `tempad w5 backfill` (`src/w5/backfill.ts`) classifies each session's windows independently: a window that throws (after one retry) is logged as `backfill: failed <session> window <n>: <message>`, counted in `failed`, and does not stop the rest of that session or later sessions. The "already covered" check queries an explicit `w5_windows` table (`session_id, started_at, ended_at`), populated by a `window.classified` event appended right after a chunk is successfully applied (`src/intent/projections/window.ts`) -- not by looking at the traces a chunk produced, since a chunk the classifier splits into 2+ segments yields 2+ traces, none of which individually spans the chunk's exact bounds. A session partially processed by a crashed run only has its actually-classified windows skipped; the rest are (re)classified on the next run, so a crash never causes silent gaps or duplicate windows. Windows skipped this way are counted in `windowsSkipped`. The command exits 0 as long as at least one window was classified, and 1 only when every attempted window failed; the summary line is `classified=<sessions> windows=<succeeded> failed=<failed> skipped=<sessions already covered> windows_skipped=<windows already covered>`.

**Declared quests.** `[w5].mode` (`"declared"`, default, or `"inferred"`) switches the classifier between verifying a session's own declared quests (`tempad quest declare`, the `tempad-quest` skill) and today's inference behavior; `[w5].inference_fallback` (default `true`) lets a session that never declares anything fall back to inference during backfill. See `docs/specs/2026-09-07-declared-quests-design.md` and `docs/specs/2026-09-07-stints-design.md`.

**Two prompts, one per mode.** `buildSystemPrompt(mode)`/`buildUserPrompt(window)` render the verifier schema (`belongs`/`guess`, `quest`/`stint`, the active quests with their plans, stint aliases) in declared mode and the pre-verifier schema (`matchedQuest`/`proposedQuest`/`questions`, open quests, recent side quests, real stint ids) in inference mode; `ClassifierWindow.mode` carries the choice from `buildWindow` to both classifiers, and `validateResult` keys off it too — inference-mode segments have no `belongs` field and their selectors are not alias-checked. The inference path's `apply.ts` reads `matchedQuest`/`proposedQuest`, so rendering the verifier prompt there would leave every quest unresolved and silently defeat `inference_fallback`. Both fixed prompts stay under 2 KB. Fakes that hand-return a `ClassifierResult` bypass both functions, so the per-mode rendering has its own tests.

**Verifier mode.** In declared mode the classifier never chooses a quest — it verifies. `ClassifierWindow` carries `activeQuests`/`activeQuestAliases`, `parentActiveQuests`/`parentActiveQuestAliases` (a subagent's parent), `openStintAliases` and `planAliases`; `openQuests`/`recentSideQuests` are built only in inference mode, so declared-mode windows have no quest lists at all. Each `ClassifierSegment` answers `belongs` (does this stretch serve one of the active quests) with a `guess` naming what it looks like instead — required when `belongs` is false, forced back to `null` when it is true — plus `quest` (which active quest it serves) and `stint` (where it lands). `matchedQuest`/`proposedQuest` remain on the type but are inference-only.

**A session accumulates declarations.** Every `tempad quest declare` marks its quest **active** for the session from that moment on; declaring a second quest no longer retires the first. `activeDeclaredQuests(database, { sessionId, at })` (`src/intent/declarations.ts`) folds the session's `quest.declared` events in order — a plain declaration adds or amends a quest's plan, one carrying `done: true` removes it — and returns the survivors oldest-first with fresh `Q1..Qn` aliases (assigned per call over the surviving set, never persisted). `tempad quest declare --session <id> --done <quest>` writes that ending event; `--done` is mutually exclusive with `--quest`/`--new`/`--plan`/`--origin`. `currentDeclaredQuest` is untouched and still resolves the single latest declaration for the callers that only want one (`report/intent-queries.ts`'s non-Claude attribution). The hook context line (`buildDeclarationLine`, `w5/hooks.ts`) now reads `active quests: <title> [Q1], <title> [Q2]` — or `none` — and the `belongs` hand-back lists the same set, since a doubt is raised against all of them at once.

**The plan defines the grain.** A declaration's `--plan` is the list of **stints** the verifier matches against before it may propose a new one, never a list of maneuvers ("read the brief; edit the file; run tests" is one stint, not three). The prompt lists each active quest's plan as `P<n>.<m>` (numbered per quest: `Q2`'s items are `P2.1`, `P2.2`) alongside the session's open stints as `S1..Sn`. A segment's `stint` is one string with a structural prefix — `P2.1`, `S1`, or `new: <one line>` — rather than three optional fields, because the candidates now come from two different sources that the value itself has to disambiguate. A plan item is prompt-only until a segment names it: `apply.ts` then opens one stint for that (quest, plan item) carrying `plan_index` (`stints.plan_index`, migration `0013_plan_stints.sql`) and reuses it for every later segment naming the same pair, in this window via an in-memory map and across windows via a `plan_index` lookup; coming back after it closed opens a stint that `continues` it. The latest plan declared for a quest in the session wins, since `activeDeclaredQuests` keeps amending each quest's stored plan.

**Aliases.** Declared-mode windows label candidate stints `S1..Sn` in list order (session-open first, then recent) and return `openStintAliases` as the reverse map; active quests are `Q1..Qn` (`activeQuestAliases`) and a subagent additionally sees its parent's as `PQ1..PQn` (`parentActiveQuestAliases`), which is a valid target for `segment.quest` since the two prefixes never collide. The prompt never contains a ULID, which closes the "garbled 26-character id" failure mode by construction. `validateSegment` repairs anything the window never offered rather than rejecting the window: an unknown `stint` selector becomes `"new: <what>"`, and an unknown `quest` alias forces `belongs: false` with a default `guess` of `"unrecognized quest alias"` so the doubt still reaches a human — both count the existing `selectorDefaulted`, since they are the same "the model named something not on the list" failure. `apply.ts` maps aliases back before touching a row. Inference mode keeps real ids in the slice, since its `apply.ts` path looks them up directly.

**Doubts and the two question kinds.** `AppliedSummary.questConflicts` and `BackfillResult.questConflicts` are both named `doubts` (and `w5 eval` prints `doubts=`); in declared mode it counts `belongs: false` segments, in inference mode the old quest disagreement — a run is one mode or the other, so the two meanings never coexist. `QuestionKind` is `"belongs" | "declare"`. A `belongs: false` segment records a doubt (even when asking is disabled, so backfill and `tempad review` still show it) and, when asking is on, asks a `belongs` question carrying the segment's `guess` in the `questions.guess` column (`0010_verifier_questions.sql`). A declared session with nothing declared _yet_ records its traces with `quest_id = null` and asks exactly one `declare` question for the whole window — an undeclared window is one gap, not N. In a subagent session both questions are addressed to the parent session, which is the one that can answer. Hand-back sentences are rendered fresh at read time by `buildBelongsHandback`/`buildDeclareHandback` (`w5/hooks.ts`) from kind + guess + the quests active _now_ (listed with their `Q` aliases), never from the stored `text`, which is internal for these two kinds; `w5 context` and `tempad review` both render them, so an expired doubt stays answerable. `tempad answer` takes exactly one of `--belongs` (confirms the quest, moves nothing) or `--quest`, the latter optionally with `--origin current --trigger ... --kind ...` to record the side quest the answer reveals.

**Nothing is inferred in declared mode.** `applyResult` never appends `quest.created`, `quest.branched` or an `stint.assigned` that reassigns a matched stint's quest; the declared quest is attached only to a stint that has none. `resolveQuest`/`createQuest`/`branchQuest` stay in `apply.ts` for the inference path and are simply not called from the declared one. The per-session fallback decision is made once, before backfill's chunk loop: a session that declares nothing _ever_ runs inference (its quests get `origin_kind: "inferred"`), while a session that declares partway through runs declared for its whole span — its earlier chunks take the `declare`-question path. That per-session rule is exactly what keeps inferred quests off a declared session's stints. A live `runOnce` never falls back: `inference_fallback` is backfill's alone.

**Retractions.** Events are append-only, so a wrong or duplicate fact is corrected by a later `retracted` event, never by editing history. A `retracted` event's `subject` is the id of the row it retracts (a trace, stint, or quest id) and its `payload` carries `{ retracts: <event id>, reason }`; the `stints` and `quests` projections (`src/intent/projections/stint.ts`, `quest.ts`) each try an `UPDATE ... SET retracted_at = ? WHERE id = ?` for the tables they own, so exactly one table's row is ever touched per retraction. Reports (`src/report/intent-queries.ts`) and `tempad review`/`w5 review` filter `retracted_at IS NULL` throughout, so a retracted trace/stint/quest disappears from minutes, evidence, and review output without its history being erased.

**`tempad w5 dedupe [--dry-run]`** (`src/w5/dedupe.ts`) is the fix for the backfill duplication incident: it groups live traces by `(session_id, started_at, ended_at)`, keeps the earliest (lowest event id) trace in each duplicate group, and retracts the rest with reason `duplicate backfill window`. It then retracts stints left with no live trace and unconfirmed quests left with no live stint, both with reason `orphaned by dedupe`, cascading through whatever the trace retraction orphaned (e.g. a whole second stint/quest opened by a re-run of the same window). `--dry-run` computes and prints the same counts without writing. Safe to run repeatedly -- a clean database dedupes to `traces=0 stints=0 quests=0`.

**Stint lifecycle.** TemPad tracks the attention of the human, not of the agent: one session (a team lead coordinating two quests, a session running parallel subagents) legitimately holds several stints open at once, so they interleave rather than exclude each other. A `stint.closed` event's payload carries a `reason` (`"idle" | "session_end" | "switch"`), stored as `stints.close_reason`: `reason` says why the stint stopped being open. (A close-time `outcome` judgment column existed before the 2026-09-07 rename but was never written; migration 0011 dropped it, and `stints.outcome` now holds what the stint pursued.) `switch` stays in the enum and projection for events emitted before this change, but nothing emits it anymore — a switch closes nothing. `src/w5/lifecycle.ts` emits the two reasons still in use: `closeIdleStints` closes any open stint of a session whose last trace ended more than `[w5].stint_idle_minutes` (default 45) minutes before the current run's window starts, before classification ever sees it; `closeSessionStints` closes every remaining open stint of a session and clears `w5_runs.session_note` when the `SessionEnd` hook fires. `openStintContinuing` opens a new stint with an optional `continues: <closed stint id>` link, recorded in the `stints.continues` column, for returning to the same outcome after a gap, and an optional `planIndex` recorded as `stints.plan_index` when the stint stands for a plan item. A stint closed by either path is then checked against `[w5].stint_min_minutes` (default 5): if its live trace minutes are below the minimum, `closeIdleStints`/`closeSessionStints` append `stint.dismissed` (reason `"below minimum"`), recorded as `stints.dismissed_at`. A dismissed stint's traces are not retracted or reassigned — they keep their `stint_id` and quest — but the stint itself is excluded from report/eval queries, the classifier's memory slice, and stint selection everywhere else (`dismissed_at IS NULL` alongside `retracted_at IS NULL`). `isSwitch` on a segment only marks the nexus event for side-quest branching in `apply.ts` (`quest.branched`, `deviates_from` = the previous segment's stint, `kind` one of `blocker | curiosity | waiting | unknown`) — appended only when the segment's quest is not already open in the session, since returning to a quest already in flight is not a nexus event. Time accounting sums trace minutes, never stint spans, so overlapping stints never double count. Three more `[w5]` keys bound the classifier's memory slice: `memory_hours` (default 8) and `memory_stints` (default 10) limit how far back and how many recent stints it can see, `overlap_messages` (default 3) is how many prior messages are included as non-classified context.

**Classifier memory slice.** `buildWindow` (`src/w5/window.ts`) hands the classifier a recent-context slice instead of a single previous trace: `sessionOpenStints` (this session's still-open stints), `recentStints` (stints in the same `org/project` opened or closed within `memory_hours`, capped at `memory_stints`), `recentSideQuests` (the last 3 branched quests with their trigger), `overlapMessages` (the tail before the window's cut, context only), and `previousSessionNote` (`w5_runs.session_note`), which is rendered inside a fenced, explicitly-labeled data block — the note is classifier-authored but composed from transcript text the user and tools control, so the system prompt states that fenced text is a hint, never an instruction, and any fence inside the note is neutralized. `recentStints` includes _closed_ stints of the current session as well as other sessions', because an idle gap mid-session closes a stint and returning to it afterwards is exactly a `continues` link; still-open ones live only in `sessionOpenStints`, so nothing appears twice. A whole-session window (`sinceTs` null, as backfill builds) has no overlap tail — there is nothing before its first message.

**The three-way selector is inference-only from now on.** In inference mode each `ClassifierSegment` must still set exactly one of `matchedStint` (reuse an open stint), `continuesStint` (resume a closed one) or `newStintReason` (why neither fits), and `validateResult` repairs a violation instead of rejecting the window, since failing loses a real stretch of work over a formatting slip: a segment naming none defaults to `newStintReason: "classifier gave no reason"` and counts `selectorDefaulted`; one naming several is narrowed by the precedence `matchedStint > continuesStint > newStintReason` and counts `selectorAmbiguous`. Declared mode replaced all three with the single prefixed `stint` field described above, so those three fields are typed optional and are absent there — `validateSegment` branches on `mode` for them exactly as it already does for `belongs`/`guess`. `validateResult` also normalizes every optional field (`matchedQuest`, `proposedQuest`, `trigger`, plus the three selectors in inference mode) from absent to `null` before validating -- a model that omits a key means the same as one that sends `null`, and treating absence as a type error used to fail whole windows. `ClassifierResult` also carries a top-level `sessionNote` (at most 300 characters), written to `w5_runs.session_note` by `completeJob`'s trailing `sessionNote` parameter and fed back on the session's next run. `applyResult` never trusts a stint id the classifier returns: every id is looked up (ignoring retracted rows) before use. `matchedStint` is honoured only for a stint that is still open; `continuesStint` only for one that has closed — pointed at a still-open stint it is treated as a plain reuse, since opening a second row would leave two open stints for one outcome. An id that resolves to nothing falls through to opening a new stint and counts an `unknownStintIds`. `applyResult` also never reassigns a matched stint's quest, but only a real disagreement counts: a `questConflict` needs `matchedQuest` to be a **non-null** id different from the stint's own. `matchedQuest: null` is _no opinion_, not "no quest" -- the stint keeps its quest and nothing is reported, without which nearly every segment counted as a conflict. The one case where a matched stint's quest does change is a gap rather than a contest: `proposedQuest` on a matched stint that has **no** quest creates it and attaches it through the ordinary `stint.assigned` path, counting `questProposedOnMatched`. The summary carries `doubts` (`questConflicts` before the verifier landed), `overlapDropped` (segments discarded for falling entirely inside the overlap range), `unknownStintIds` and `questProposedOnMatched`. Everything in this paragraph from `matchedQuest` onward describes the **inference** path only; declared mode makes no quest decision at all.

`buildWindow` takes a `windowEnd` -- the last message timestamp of the window being classified -- and every candidate is bounded by it: a recent or session-open stint must have `opened_at < windowEnd`, and a recent one must also still be open or have closed within `memory_hours`. Backfill walks history, so without this bound a window on Sept 1 was offered stints opened on Sept 3 by a session processed earlier; the classifier matched them, the traces it recorded landed before their stint's `opened_at` (negative measured durations), and `continues` could almost never fire. A live run passes `now`, for which the bound is a no-op -- nothing in the database is in its future.

Relatedly, a stint's `opened_at` is the **segment's** `startedAt`, not the run's wall clock: backfill classifies history with `now` set to the moment the run happens, and stamping `opened_at` with that put every backfilled stint days after the traces it owns.

Backfill rebuilds the slice **per chunk**, not once per session: chunk n+1 must see the stints chunk n opened or closed and the note it wrote, exactly as a live run sees the previous hook invocation's work. Its `sinceTs` is the previous chunk's last message so the overlap tail is the messages before the chunk, never the chunk's own — `messages` is then overridden with the chunk. `writeSessionNote` (`src/w5/jobs.ts`) is the shared note write used by both backfill's per-chunk loop and the runner's `completeJob`.

Ordering matters in `runOnce` (`src/w5/runner.ts`): `closeIdleStints` runs **before** `buildWindow`, so the slice never offers a stint that idleness already ended — the window's start comes from a direct `MIN(ts)` query rather than the built window.

**`tempad w5 eval --from <date> --to <date> [--db <path>] [--declare <file>]`** (`src/w5/eval.ts`) measures the stints/traces ratio against real history without ever writing to the real database: `--from`/`--to` are validated as parseable dates with `from <= to` (rejecting bad input with a message and exit 1) before anything touches disk. `validateEvalRange` then normalizes both into an exclusive-`to` range (`normalizeEvalRange`): a bare date (`"2026-09-02"`, no time component) resolves to that day in the configured `TZ` (via `localDayBoundsUtc`, the same helper reports use) — `from` becomes that local day's UTC start and `to` becomes the _next_ local day's UTC start; a full ISO input is trusted as given. `w5 eval` also prints `sessions_declared=`/`sessions_inferred=` (from `backfill`'s per-session `modeFor`), so a mixed declared/inferred run is no longer invisible. Every downstream comparison on the upper bound uses strict `<` against this normalized `to` (in both `eval.ts` and `backfill.ts`), so a bare `--to 2026-09-02` reads as "through the end of Sept 2", not "through Sept 2 at midnight" — a bare date compared with `<=` as TEXT against full ISO timestamps would otherwise silently exclude the entire `to` day, since any timestamp on that day sorts lexicographically after the bare date string. The source (`--db`, else `TEMPAD_HOME/tempad.db`) is copied to `TEMPAD_HOME/scratch/eval-<timestamp>.db` via SQLite's own `VACUUM INTO` on a read-only connection — not a file-level copy, since the real db runs in WAL mode and a copied `.db` file alone can miss committed rows still sitting in the uncheckpointed `-wal` sidecar. `--declare <file>` (TOML or JSON, by extension) lists `{ session_id, at, quest? | new? }` entries applied as `quest.declared`/`quest.created` events against the **copy** right after it opens — so history can be evaluated in declared mode without the source database ever holding hand-authored declarations; the copy's hero row is used, or a fresh one is seeded if the copy has none. Before rerunning, on the **copy only**, `resetRange` retracts (reason `eval reset`) every live trace whose `started_at` falls in `[from, to)`, then stints left with no live trace, then unconfirmed quests left with no live stint — the same cascade `dedupe.ts` uses, but selected by date range instead of duplicate grouping, since an earlier run's backfill for the same days already left its own traces/stints/quests in the source database, and without this reset the eval would mix that old cohort into the rerun's numbers and offer its stints to the classifier as reuse candidates. It also clears `w5_runs.session_note` and deletes `w5_windows` rows for the sessions touched, since the copy is throwaway and the rerun's `force` flag bypasses `w5_windows` coverage but would otherwise leave the copy inconsistent with what it records. It prints `eval: reset traces=<n> stints=<n> quests=<n>` before the rerun and `retracted_before_rerun traces=<n> stints=<n> quests=<n>` alongside the metrics. It then force-reclassifies every window in `[from, to)` against the copy via `backfill`'s `force`/`from`/`to` options (`force` bypasses `w5_windows`/legacy coverage entirely; `from`/`to` bound both the session query and each session's per-message window, so a session that runs past `to` is only classified up to `to`). All metrics — trace count, stint count, the ratio, median stint duration, `continues` link count, and the 20 randomly sampled stints — are computed over live rows with `started_at` (traces) / `opened_at` (stints) in `[from, to)` after the reset, so they describe the rerun only; `doubts`, unknown stint ids and overlap-dropped segments are summed straight from `apply.ts`'s real per-window run summary, not a placeholder. It also prints `doubts_answered` (belongs/declare questions resolved in range), `traces_unattributed` (traces whose stint has no quest), `declarations_skipped` (see below), `quest_proposed_on_matched`, `selector_defaulted` and `selector_ambiguous`, so a run shows how often the classifier omitted the quest on a stint that lacked one and how often it broke the exactly-one selector rule.

**Non-Claude attribution and origin_kind in reports.** `attributeNonClaudeEvidence` (`report/intent-queries.ts`) resolves each `gh_commits`/`monday_items` row in range to a quest at report time — never in the collectors, which stay pure mirrors — by joining the row's org/project to the `claude_sessions` row active at its timestamp (latest-started wins on overlap, a documented heuristic) and resolving that session's `currentDeclaredQuest`; a row with no overlapping session or no declaration at that instant is unattributed (`questId: null`), never invented. This is wired into both `daily.ts` (commit lines and Monday item lines gain an ` — <quest title>` suffix) and `project.ts` (the branch-row and Monday-row task-name cell gains the same suffix, with a branch's own commits searched in order for the first one that resolves), not just exercised by its own unit test. `queryStints`/`queryQuests` also carry `questOriginKind`/`originKind`, rendered by `daily.ts`/`project.ts` as an `(inferred)` suffix on the quest title whenever `originKind !== "declared"` — a declared quest gets no annotation, matching how `questConfirmed`/`[unconfirmed]` already works. `queryDoubtRows` returns one row per `belongs`-kind question in range with its org/project; `querySideQuestDoubts` sums it for a single org/project, and `weekly.ts` fetches `queryDoubtRows` once per day (like `stints`/`quests`/`sideQuests`) and filters it in-memory per project key, rather than re-querying per (day × project) pair.

**`w5 eval --declare` file handling.** `applyDeclareFile` (`w5/eval.ts`) wraps the file read and JSON/TOML parse in `InvalidDeclareFileError` — a missing file, malformed JSON/TOML, or a non-array top level all surface as `--declare file ...` messages with exit 1 (`runEvalCommand` in `w5/cli.ts` catches the error the same way `validateEvalRange`'s `InvalidEvalRangeError` already is), never a raw stack trace. Each entry may set `scope`/`parent_session_id`/`declared_by` (defaulting to `"session"`/absent/`"hero"`) alongside `session_id`/`at`/`quest`/`new`, passed straight through to `declareQuest` — so a file can declare a subagent's quest against its parent. An entry's `new` may carry a sibling `new_ref: "<key>"`, and a later entry may use `quest_ref: "<key>"` instead of `quest` to reuse the quest a prior entry just created; refs resolve in file order and an unknown `quest_ref` is an `InvalidDeclareFileError`. A `session_id` absent from the copy's `claude_sessions` still appends its `quest.declared` event (declarations are events, not FK-checked) but can never resolve to a trace/window, so it is logged and counted in `EvalMetrics.declarationsSkipped` (printed as `declarations_skipped=`) rather than silently accepted.

## Gotchas

- Monday.com's API `people` column filter by numeric id returns nothing. Filter client-side by name or id from the column's JSON value.
- Claude session `.jsonl` files mix timestamped message lines with untimestamped metadata lines (`custom-title`, `agent-name`, `bridge-session`). Join them on `sessionId`.
- Git history rewrites: collectors re-scan an overlap window and delete rows whose sha is no longer reachable from any ref in the mirror.
